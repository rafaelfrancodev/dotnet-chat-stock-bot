@page
@model ChatModel
@{
    ViewData["Title"] = "Chat";
}

<h1>@ViewData["Title"]</h1>

<p>You are signed in as <strong>@Model.DisplayName</strong>.</p>
<p class="text-muted">The message history and the live connection are added next.</p>
